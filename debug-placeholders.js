const text = '\\sqrt{$1} $0';

// Simulate the placeholder processing
const placeholderRegex = /\$(\d+)/g;
let match;
while ((match = placeholderRegex.exec(text)) !== null) {
    // Found placeholder: match[0] at position match.index
}

for (let i = 0; i < text.length; i++) {
    // Position i: text[i]
}
